feat(frontend,backend): деталка новости, источник, похожие; валюта/погода; RSS и категории; вход в админку

Frontend:
- NewsDetailPage: правильные внутренние ссылки в «Похожих», безопасный внешний fallback; «Подробнее в источнике» + имя источника; источник в «Похожих»; сжатие центральной колонки слева под ширину правого сайдбара; убран дубль картинки; фолбэк на SVG-заглушку; API_URL для статики; ESLint-правки (rel="noreferrer", unused vars).
- HeaderInfo: исправлен и рабочий дропдаун валют; показываем только выбранную валюту; погода на русском; вывод города по-русски; выбор города (localStorage) + геолокация; возвращено положение блока в шапке; фикс «курсы в копейках».
- Navbar: корректный переход в админку через токеновую схему.
- FeedPage: устранены ESLint-предупреждения.
- NewsCard: ликвидирован импорт несуществующего useThemeFallback.
- AuthorDashboard: фикс .map при пустом списке.

Backend:
- import_rss: чистка HTML, улучшенный summary; поддержка NewsSource (название/сайт/логотип); логотип источника как фолбэк для новостей без фото; автосоздание категорий; диагностические сообщения.
- serializers: полный файл; CategorySerializer с news_count, лёгкий CategoryMiniSerializer, унифицированный сериализатор News.
- security: правки admin_entrypoint — истечение токена/редирект; согласовано с Navbar.
- mgmt-команды: fix_empty_summaries (гарантия абзаца в summary); merge_duplicate_categories (слияние «Новости»/«Общее»/пустых в «Лента новостей», перенос из категорий <10 новостей, удаление пустых); улучшена диагностика classify_uncategorized_news.

Assets:
- Добавлена SVG-заглушка default_news.svg и подмена вместо PNG.

Style:
- Мелкие правки под CSS-токены (--bg-card, --fg, --border) для единообразия.

Миграции:
- Если менялась модель источников/категорий — включены новые миграции.

Refs: RSS ingestion, NewsSource, tokens, admin token login, categories cleanup
